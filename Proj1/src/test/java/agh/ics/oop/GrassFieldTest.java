package agh.ics.oop;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class GrassFieldTest {
/*
    @Test
    public void GrassFieldTest1(){
        String[] args = new String[]{"f", "b", "r", "l", "f", "f", "r", "r", "f", "f", "f", "f", "f", "f", "f", "f"};
        MoveDirection[] directions = new OptionsParser().parse(args);
        AbstractWorldMap map = new GrassField(10);
        Vector2d[] positions = { new Vector2d(2,2), new Vector2d(3,4) };
        IEngine engine = new SimulationEngine(directions, map, positions);
        System.out.println(map);
        engine.run();
        System.out.println(map);
        Animal[] animals = engine.getAnimals();

        Assertions.assertEquals(animals[0].getPosition().toString(), "(2,-1)");
        Assertions.assertEquals(animals[1].getPosition().toString(), "(3,7)");
        Assertions.assertEquals(animals[0].toString(), "S");
        Assertions.assertEquals(animals[1].toString(), "N");
    }

    @Test
    public void GrassFieldTest2(){
        String[] args = new String[]{"f", "f"};
        MoveDirection[] directions = new OptionsParser().parse(args);
        AbstractWorldMap map = new GrassField(10);
        Vector2d[] positions = { new Vector2d(2,2), new Vector2d(3,4) };
        IEngine engine = new SimulationEngine(directions, map, positions);

        Assertions.assertEquals(map.isOccupied(new Vector2d(3,5)), true);
        Assertions.assertEquals(map.canMoveTo(new Vector2d(3,5)), true);


        System.out.println(map);
        engine.run();
        System.out.println(map);
        Animal[] animals = engine.getAnimals();

        Assertions.assertEquals(animals[0].getPosition().toString(), "(2,3)");
        Assertions.assertEquals(animals[1].getPosition().toString(), "(3,5)");
        Assertions.assertEquals(animals[0].toString(), "N");
        Assertions.assertEquals(animals[1].toString(), "N");
    }
*/

}
